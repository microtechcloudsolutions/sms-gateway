'use strict';


const Queue = require('../index');

const queue = new Queue('failer');

queue.process(
  async function(job) {
    throw Error('This processor will always fail');
  }
);

queue.add({ foo: 'bar' });
